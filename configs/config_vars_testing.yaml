worker-threads: 4
worker-fs-threads: 2
logger-level: debug

is_testing: true

server-port: 8080

# lavka_db_1 is the service name + _ + filename of the couriers.sql
dbconnection: 'postgresql://testsuite@localhost:15433/lavka_couriers'
